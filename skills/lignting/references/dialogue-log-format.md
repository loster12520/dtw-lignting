# 对话记录格式

小阿卡那各层都产出一份**对话记录**，与同层的**方案文档**并列存放。方案文档记录"定了什么"，
对话记录记录"怎么定下来的、否决了什么、还有什么没想清"。

## 定位（先读这条）

| | 方案文档 | 对话记录 |
|---|---|---|
| 性质 | 拍板后的快照 | 追加式流水 |
| 读者 | 下一层的命令 | 人（未来回看时） |
| 变更 | 可重写、可被下一层推翻 | **只追加，不改写历史轮次** |
| 状态 | 不含状态 | **不含状态**（见下） |

### 硬规则：不写状态

**对话记录里不出现任何"状态字段"**——不写 `status: done`、不写"待办/已完成"标记、
不维护"当前进度"。

理由：状态真相只有一处，就是 whatsnext 的 `index.md` frontmatter 和它的 `plan.md` 复选框
（见 [whatsnext-bridge.md](whatsnext-bridge.md)）。两边都记状态必然漂移。

「开放问题 / 已定决策 / 待验证猜想」是**内容分类**，用于让人快速定位聊过什么，
**不是任务状态台账**——不要给它们加状态字段，不要标"已解决/未解决"以外的元数据。

## 文件命名与位置

与同层方案文档同目录、同主名，后缀 `.dialogue.md`：

```
.lignting/<feature>/
  direction.md              # /lts 方案文档
  direction.dialogue.md     # /lts 对话记录
  phases.md
  phases.dialogue.md
  phase-1/
    round-1.md
    round-1.dialogue.md
    round-1.report.md       # /ltw 执行汇报
```

## 结构

```markdown
# <层名> 对话记录 · <feature> · <文档主名>

## 索引

### 开放问题
- Q1 <一句话问题>（见 L2）
- Q3 <一句话问题>（见 L4）

### 已定决策
- D1 <一句话结论>（见 L3）
- D2 <一句话结论>（见 L5）

### 待验证猜想
- H1 <一句话猜想>（见 L4）

## L1 · <该轮主题>

**问**：<用户原话或要点，可精简但不要改意>

**答**：<回应要点>

<正文：展开的论证、举例、反例、代价分析>

## L2 · <该轮主题>

...
```

## 写法要求

- **索引区放最前**，只列条目 + 回指编号，不写解释。新读者靠它跳到关心的轮次。
- **轮次编号 `L<n>` 只增不改**。追加新轮次时不动旧轮次；发现旧轮次写错了，
  在新轮次里更正并注明（"更正 L3：……"），不要回头改写历史。
- **保留用户原话的语义**。可以精简重复，但不能把用户的犹豫、疑问改写成肯定句——
  那些犹豫往往正是后面决策的依据。
- **被否决的方案要留在正文里**，附当时的否决理由。这是对话记录最值钱的部分，
  方案文档不会记它。
- **标注确定度**：`[事实·依据]` / `[推断·依据]` / `[猜想·待验证]`（规范见各命令文件）。

## 何时写

- **每轮讨论结束就追加**，不要攒到最后一次写。攒着写等于丢掉中间过程。
- 用户明说"别记"时跳过本轮。
- 追加前若文件不存在，按上面结构新建（先建索引区再写 `L1`）。
